services:
  postgres:
    image: postgres:17
    container_name: enterprise_risk_postgres
    environment:
      POSTGRES_DB: enterprise_risk_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
