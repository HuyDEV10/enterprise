import { useState } from 'react'
import { Navigate,useLocation,useNavigate } from 'react-router-dom'
import { useAuth } from '../auth/AuthContext'
import { getApiErrorMessage } from '../api/apiClient'
export default function LoginPage(){const {user,login}=useAuth();const navigate=useNavigate();const location=useLocation();const [username,setUsername]=useState('admin');const [password,setPassword]=useState('Admin@123');const [error,setError]=useState('');const [submitting,setSubmitting]=useState(false);if(user)return <Navigate to="/dashboard" replace/>;async function submit(e){e.preventDefault();setSubmitting(true);setError('');try{await login({username,password});navigate(location.state?.from?.pathname||'/dashboard',{replace:true})}catch(err){setError(getApiErrorMessage(err,'Tên đăng nhập hoặc mật khẩu không đúng.'))}finally{setSubmitting(false)}}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#f5f7fb',padding:24}}><form onSubmit={submit} style={{width:'100%',maxWidth:420,background:'#fff',padding:32,borderRadius:16,boxShadow:'0 12px 40px rgba(15,23,42,.12)'}}><h1 style={{marginTop:0}}>Enterprise Risk System</h1><p>Đăng nhập để truy cập hệ thống.</p>{error&&<div style={{padding:12,background:'#fee2e2',borderRadius:8,marginBottom:16}}>{error}</div>}<label>Tên đăng nhập<input value={username} onChange={e=>setUsername(e.target.value)} required style={{width:'100%',padding:10,margin:'6px 0 16px'}}/></label><label>Mật khẩu<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required style={{width:'100%',padding:10,margin:'6px 0 16px'}}/></label><button disabled={submitting} type="submit" style={{width:'100%',padding:12}}>{submitting?'Đang đăng nhập...':'Đăng nhập'}</button><small style={{display:'block',marginTop:16}}>Tài khoản dev mặc định: admin / Admin@123</small></form></main>}
