import { useNavigate } from 'react-router-dom'
import { useAuth } from '../auth/AuthContext'
export default function Header(){const {user,logout}=useAuth();const navigate=useNavigate();function signOut(){logout();navigate('/login',{replace:true})}const initials=(user?.fullName||user?.username||'U').split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase();return <header className="top-header"><div><strong>Enterprise Risk System</strong><span>Giám sát chuỗi cung ứng & rủi ro doanh nghiệp</span></div><div className="header-user"><span className="avatar">{initials}</span><div><strong>{user?.fullName||user?.username}</strong><small>{user?.roles?.join(' · ')}</small></div><button type="button" onClick={signOut}>Đăng xuất</button></div></header>}
